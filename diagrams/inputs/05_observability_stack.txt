Create an observability architecture diagram showing two independent telemetry paths, flowing left to right.

Left: a green rounded rectangle "Agent Engine (ADK agent)" with a badge "OpenTelemetry auto-instrumentation".

Path A, upper, labeled "Platform-emitted (always on)":
- Arrow to a blue "Cloud Trace" box: "agent / tool / model span tree"
- Arrow to a blue "Cloud Logging" box: "structured runtime logs"
- Arrow to a purple "Cloud Monitoring" box: "reasoning_engine/request_latencies, request_count"

Path B, lower, labeled "Self-reported (only while eval publishers run)":
- A green box "Eval publishers" arrow to a purple "Cloud Monitoring" box containing three stacked labels:
  "custom.googleapis.com/agent_eval/*", "custom.googleapis.com/agent_online_eval/*", "custom.googleapis.com/agent_router/*"

Right: a red octagon "Alert Policies" with two grouped lists:
- "Self-reported: quality floors, infra_empty_rate ceiling"
- "Managed engine health: p99 latency > 5s, 5xx rate > 5%"
Arrow from Alert Policies to a gray box "Notification channel".

A gray dashed box at the bottom, faded, labeled "BigQuery agent analytics - DORMANT (opt-in, not wired)".

Callout box: "The managed engine metrics page even with no client-side instrumentation running. The custom series exist only while a publisher runs."

Style: professional, clean architecture diagram in the style of official Google Cloud Platform documentation. GCP brand colors: blue (#4285F4), green (#34A853), yellow (#FBBC05), red (#EA4335). Clean white background. Google Cloud product icon style, clean lines, no 3D effects, no hexagons, modern flat design. Rounded rectangle = service, cylinder = database, ellipse = users, octagon = security control. Google Cloud logo watermark at bottom left. Do not put quotation marks around any label text.
Title at top: "Observability: Platform Metrics + Self-Reported Quality"
