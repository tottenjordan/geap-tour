Create a security diagram showing layered prompt screening for an AI agent, laid out left to right.

Left: a red ellipse "User prompt".

Layer 1, a red octagon labeled "Client-side guardrail (ALWAYS ACTIVE)":
- Subtitle "input_guardrail_callback"
- Bullet list: "blocklist patterns", "length limits", "emits guardrail.blocked span + metric"
- Two outgoing arrows: a red one labeled "blocked" to a red box "Rejection message", and a green one labeled "allowed" continuing right.

Layer 2, a prominent yellow decision diamond labeled "Which backbone?" feeding two mutually exclusive server-side paths:
- Green branch "Regional Gemini 2.x" to a red octagon "Model Armor templates":
  subtitle "server-side screening: prompt injection, content safety, sensitive data, malicious URLs"
- Blue branch "Gemini 3.x global endpoint, or Claude via LiteLLM" to a red octagon "ADK Model Armor plugin (ENABLE_MODEL_ARMOR_PLUGIN, default ON)":
  subtitle "request-path screening where region-scoped templates cannot attach"

A small note beside the decision: "Exactly one server-side layer is active at a time - the plugin covers the backbones the templates cannot reach, so no backbone falls back to the local blocklist alone."

Right: a green rounded rectangle "Agent Engine" then a purple rounded rectangle "Vertex AI Model".

Bottom strip, a gray bordered region "Project floor setting (inspect-only) + Cloud Logging" with an arrow to a blue box "Security dashboard".

Style: professional, clean architecture diagram in the style of official Google Cloud Platform documentation. GCP brand colors: blue (#4285F4), green (#34A853), yellow (#FBBC05), red (#EA4335). Clean white background. Google Cloud product icon style, clean lines, no 3D effects, no hexagons, modern flat design. Rounded rectangle = service, cylinder = database, ellipse = users, octagon = security control. Google Cloud logo watermark at bottom left. Do not put quotation marks around any label text.
Title at top: "Layered Prompt Screening: Client Guardrail + Model Armor"
