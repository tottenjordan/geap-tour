Create a Google Cloud deployment architecture diagram, laid out top to bottom.

Top: a red ellipse "Users / Eval Clients" with a downward arrow labeled "stream_query".

Middle, a large blue bordered region labeled "Vertex AI Agent Engine (us-central1)":
- Two green rounded rectangles side by side: "Coordinator Agent" and "Router Agent"
- Each shows three small badges underneath: "AdkApp", "cpu 4 / memory 16Gi", "min_instances 4"
- A red octagon badge on the region labeled "AGENT_IDENTITY (SPIFFE)"
- Two purple cylinders attached to the region: "Managed Sessions" and "Memory Bank"

Below, a green bordered region labeled "Cloud Run":
- Three teal rounded rectangles: "Search MCP", "Booking MCP", "Expense MCP"

Left side, a gray rounded rectangle "Agent Registry" with a dashed arrow to the Agent Engine region labeled "resolve MCP server (needs roles/agentregistry.viewer)".

Right side, a purple rounded rectangle "Vertex AI Models" listing "Gemini 2.5 / Gemini 3.x / Claude via LiteLLM".

Arrows:
- Agent Engine down to Cloud Run region, solid, labeled "streamable-http"
- Agent Engine right to Vertex AI Models, solid, labeled "generateContent"
- Agent Engine right to a "Cloud Trace" icon, dashed, labeled "OpenTelemetry"

Callout box in the corner, gray: "memory 16Gi is required on EVERY engine - the 4Gi default OOM-kills workers and returns HTTP 200 with an empty body."

Style: professional, clean architecture diagram in the style of official Google Cloud Platform documentation. GCP brand colors: blue (#4285F4), green (#34A853), yellow (#FBBC05), red (#EA4335). Clean white background. Google Cloud product icon style, clean lines, no 3D effects, no hexagons, modern flat design. Rounded rectangle = service, cylinder = database, ellipse = users, octagon = security control. Google Cloud logo watermark at bottom left. Do not put quotation marks around any label text.
Title at top: "Deployment Architecture on Vertex AI Agent Engine"
