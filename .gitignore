__pycache__/
*.pyc
.env
.env.*
!.env.example
.venv/
dist/
*.egg-info/
# Diagram outputs are committed for the workshop
# diagrams/outputs/*.png
# diagrams/outputs/*.jpeg
.uv/
*.pdf
.playwright-mcp/
.pytest_cache/

# Secrets and credentials
*.pem
*.key
*.p12
*.json.bak
service-account*.json
credentials*.json
*secret*
!scripts/toolspecs/*

# GCP raw exports (may contain env vars / secrets from other services)
docs/verification/*.raw.json

# OS / editor
.DS_Store
*.swp
*.swo
*~
eval_outputs/
eval_results_*.json

# Compiled KFP pipeline spec (regenerated by src/pipelines/submit.py)
eval_pipeline.yaml
eval_pipeline.json
# Compiled optimize pipeline spec (regenerated by src/pipelines/submit_optimize.py)
optimize_pipeline.yaml
optimize_pipeline.json
# Per-design-point specs compiled by the DOE launcher (now written under
# doe_runs/<experiment_id>/; the root pattern covers older/overridden runs)
eval_pipeline_*.yaml
eval_pipeline_*.json

# DOE run artifacts (manifests, results, compiled specs, reports)
doe_runs/

# PaperBanana diagram generation outputs
outputs/
